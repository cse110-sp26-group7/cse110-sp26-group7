Refine and improve the existing slot machine in this directory of 3 separate files: index.html (structure only, no inline styles/scripts, every section must have an HTML comment explaining its purpose), styles.css (all styles, reel containers must use overflow:hidden so symbols never escape during spin, every section must have a comment explaining its purpose), and javascript.js (all logic in a single state object, no setTimeout chains deeper than 2 levels, every function must have a comment explaining what it does) — fix any bugs found, ensure reset clears all timers, restores 100 tokens as the starting balance, re-enables buttons, snaps reels to default, maintain the current color palette, and maintain and refine the 4 sounds using within the files. Ensure that there is the ability to change the bet amount, and the images used for the spinning are actual JPEGs used in casino slot machines, except in AI theme. Fix or even remove if necessary the animation where when you match the images, the images "emphasize" themselves. Currently they might bug and do it incorrectly. Lastly the spinning animation breaks after one spin, try to fix it, or make it constant.